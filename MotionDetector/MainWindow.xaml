<Window x:Class="MotionDetector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        Title="Motion Detector" Height="800" Width="1000"
        Background="#1E1E1E" Foreground="White">

    <Window.Resources>
        <Style x:Key="DarkHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Background="#2D2D2D" BorderBrush="#555"
                                          Foreground="White"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                          HorizontalContentAlignment="Left">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="#2D2D2D" BorderBrush="#555" BorderThickness="1" CornerRadius="3">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0" Margin="8,4"/>
                                                <TextBlock Grid.Column="1" Text="▼" Foreground="White"
                                                           VerticalAlignment="Center" HorizontalAlignment="Center"
                                                           FontSize="10"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              Margin="8,4,24,4"
                                              VerticalAlignment="Center">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="White"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False">
                                <Border Background="#2D2D2D" BorderBrush="#555" BorderThickness="1">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="#2D2D2D"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="8,4"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0078D4"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="🔐 Motion Detector" FontSize="24" FontWeight="Bold"
                   Foreground="#00D4FF" Margin="0,0,0,20"/>

        <!-- Připojení + Status -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Port:" VerticalAlignment="Center"
                       Margin="0,0,10,0" Foreground="White"/>

            <ComboBox Grid.Column="1" ItemsSource="{Binding Ports}"
                      SelectedItem="{Binding SelectedPort}"
                      Style="{StaticResource DarkComboBox}"/>

            <Button Grid.Column="2" Content="Obnovit" Margin="10,0"
                    Click="RefreshPorts_Click" Background="#333" Foreground="White"
                    BorderBrush="#555"/>
            <Button Grid.Column="3" Content="Připojit" Margin="0,0,10,0"
                    Click="Connect_Click" Background="#0078D4" Foreground="White"
                    BorderBrush="#0078D4"/>
            <Button Grid.Column="5" Content="Odpojit"
                    Click="Disconnect_Click" Background="#AA3333" Foreground="White"
                    BorderBrush="#AA3333"/>

            <!-- Status indikátor -->
            <StackPanel Grid.Column="4" Orientation="Horizontal"
                        HorizontalAlignment="Center" VerticalAlignment="Center">
                <Ellipse Width="12" Height="12" Margin="0,0,8,0">
                    <Ellipse.Fill>
                        <SolidColorBrush Color="{Binding StatusColor}"/>
                    </Ellipse.Fill>
                </Ellipse>
                <TextBlock Text="{Binding Status}" VerticalAlignment="Center"
                           Foreground="White"/>
            </StackPanel>
        </Grid>

        <!-- Hlavní obsah -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- Tabulka logů -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="📋 Log událostí" FontSize="16"
                           Foreground="#00D4FF" Margin="0,0,0,10"/>
                <DataGrid Grid.Row="1" ItemsSource="{Binding Events}"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          Background="#2D2D2D" Foreground="White"
                          GridLinesVisibility="Horizontal"
                          BorderBrush="#444" RowBackground="#2D2D2D"
                          AlternatingRowBackground="#333"
                          ColumnHeaderStyle="{StaticResource DarkHeaderStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Datum"
                                            Binding="{Binding DisplayDate}" Width="100"/>
                        <DataGridTextColumn Header="Čas"
                                            Binding="{Binding DisplayTime}" Width="80"/>
                        <DataGridTextColumn Header="Událost"
                                            Binding="{Binding Type}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!-- Pravý panel -->
            <StackPanel Grid.Column="1" Margin="20,0,0,0">

                <!-- Status pohybu -->
                <Border Background="#2D2D2D" CornerRadius="10" Padding="20" Margin="0,0,0,15">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Status pohybu" FontSize="14"
                                   Foreground="#888" HorizontalAlignment="Center"/>
                        <TextBlock FontSize="48" HorizontalAlignment="Center" Margin="0,10">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="😴"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding MotionActive}" Value="True">
                                            <Setter Property="Text" Value="🚨"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock HorizontalAlignment="Center" FontSize="16" FontWeight="Bold">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Klid"/>
                                    <Setter Property="Foreground" Value="#44FF44"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding MotionActive}" Value="True">
                                            <Setter Property="Text" Value="POHYB!"/>
                                            <Setter Property="Foreground" Value="#FF4444"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Počítadlo -->
                <Border Background="#2D2D2D" CornerRadius="10" Padding="20" Margin="0,0,0,15">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Celkem pohybů" FontSize="14"
                                   Foreground="#888" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding MotionCount}" FontSize="48"
                                   Foreground="#00D4FF" HorizontalAlignment="Center"
                                   FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <!-- Graf -->
                <Border Background="#2D2D2D" CornerRadius="10" Padding="15">
                    <StackPanel>
                        <TextBlock Text="📈 Graf pohybů" FontSize="14"
                                   Foreground="#00D4FF" Margin="0,0,0,10"/>
                        <lvc:CartesianChart
                            Height="200"
                            Series="{Binding Series}"
                            XAxes="{Binding XAxes}"
                            YAxes="{Binding YAxes}"
                            Background="Transparent"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </Grid>
    </Grid>
</Window>